package ru.cha0sf4me.cha0sutils.Commands;


import net.minecraft.util.text.TextFormatting;
import net.minecraft.util.text.TranslationTextComponent;

// new TranslationTextComponent(translationKey, TextFormatting.LIGHT_PURPLE + source.getDisplayName().getString() + TextFormatting.BLUE).mergeStyle(TextFormatting.BLUE), true);

public class Helper {
    public static void Helper(String translationKey, String... args)
    {
        for (int i = 0; i < args.length; i++) {

        }

        return;
    }
}
